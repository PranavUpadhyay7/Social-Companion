# Shared UI Components

This project currently has no shared low-level UI primitive library. Buttons, inputs, cards, and badges are implemented inline inside feature components. Feature-specific components are cataloged in `pages.md` and reusable candidates in `extractable-components.md`.

